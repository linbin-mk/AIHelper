## 1. 更新中文系统提示词

- [x] 1.1 更新 `role` 字段：从"HTTP 请求分析助手"改为"浏览器 AI Agent"，体现自主执行、网络请求分析、页面操作、知识库、记忆管理、技能系统等完整能力
- [x] 1.2 更新 `rule1`：新增任务分类规则，区分通用知识类问题、页面功能类问题、项目代码类问题，各有不同工具策略
- [x] 1.3 更新 `rule2`：保留"请求列表为空时建议刷新页面"的引导
- [x] 1.4 更新 `rule3`：保留"并行调用工具减少轮次，搜索无结果不要反复换词"
- [x] 1.5 更新 `rule4`：保留"所有数据写入操作必须先用 request_auth 请求授权，禁止直接执行"
- [x] 1.6 更新 `rule5`：从"你可以主动使用"强化为"必须调用 activate_skill 获取完整规则后再执行"
- [x] 1.7 新增 `rule6`：功能地图优先规则——涉及页面位置/功能存在性问题时，优先搜索记忆中的功能地图文件
- [x] 1.8 新增 `rule7`：简洁回复风格规则——回复应简洁直接，避免冗长前言后语

## 2. 更新英文系统提示词

- [x] 2.1 更新 `role` 字段：英文版与中文版语义对齐
- [x] 2.2 更新 `rule1`-`rule7`：英文版与中文版语义对齐，保持自然英文表达

## 3. 验证

- [x] 3.1 验证 `buildAgentsMdContent()` 组装输出的 Markdown 格式正确——已更新函数以包含 rule6/rule7
- [x] 3.2 验证中英文切换后生成的系统提示词内容正确——中英文 `systemPrompt` 对象均已包含完整的 role + 7 条 rule
- [x] 3.3 手动测试：用户可在设置中点击「AI主提示词」入口，通过「重置为默认」刷新缓存
